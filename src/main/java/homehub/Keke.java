package homehub;

/** Defines the name and tone markers for HomeHub's household concierge. */
public final class Keke {
    /** The assistant's display name. */
    public static final String NAME = "Keke";

    /** The assistant's short role description. */
    public static final String ROLE = "your smart pantry companion";

    /** Prefix used to mark a rejected input without repeating the assistant's name. */
    public static final String ERROR_PREFIX = "⚠️ ";

    private Keke() {
        // Utility class.
    }
}
