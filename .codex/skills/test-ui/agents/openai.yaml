interface:
  display_name: "UI Test Runner"
  short_description: "Run command-line UI tests from the project test plan"
  default_prompt: "Use $test-ui to execute the command-line UI tests in test/ui-test-plan.md, compare outputs exactly, and stop at the first failure."
